import { isActiveLink } from '../../../helpers/is-active-link';
import { module, test } from 'qunit';

module('Unit | Helper | is active link');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isActiveLink(42);
  assert.ok(result);
});
